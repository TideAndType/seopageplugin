name: PHP Tests

on:
  pull_request:
  push:
    branches:
      - ttgmbapp
      - 'claude/**'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['7.4', '8.2', '8.3']
    name: PHP ${{ matrix.php }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: PHP syntax check
        run: find seo-command-center tests -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: Unit tests
        run: php tests/run-tests.php
